const { expect } = require("chai");
const { ethers, upgrades } = require("hardhat");

describe("AutID", function () {
    describe("mint", function () {
        it("BLA BLA BLA BLA BLA", async function () {
            expect(3).to.eq(3);
        })
    })
})